FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (use install instead of ci for dev to ensure all devDeps)
RUN npm install

# Copy source (will be overridden by volume)
COPY . .

# Run vite dev server, exposing to host
CMD ["npm", "run", "dev", "--", "--host"]
